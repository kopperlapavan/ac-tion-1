name: my first worlflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-letest
        steps:
        - name: wellcome massage
          run: echo  "my first worlflow"

        - name: list files
          run: ls

        - name: read file
          run: cat README.md

        